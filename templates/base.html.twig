<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Mon Application{% endblock %}</title>
    <!-- Inclusions CSS et JS de Bootstrap -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500&display=swap" rel="stylesheet"> <!-- Police Roboto -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">

    <style>
        body {
            margin: 0;
            font-family: 'Roboto', sans-serif;
            background-color: #E9ECEF; /* Couleur de fond claire pour un contraste agréable */
            height: 100vh;
            display: flex;
            flex-direction: column;
        }

        /* Top Bar */
        .top-bar {
       background: linear-gradient(135deg, #580800,rgb(197, 145, 102), #580800);


/* Dégradé bleu-violet */
           color: #fff;
            padding: 10px 10px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            position: fixed;
            top: 0;
            width: 100%;
            z-index: 1000;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }

        .top-bar .logo {
            font-size: 1.8rem;
            font-weight: bold;
            text-transform: uppercase;
            color: #fff; /* Logo en blanc pour ressortir sur le fond coloré */
            display: flex;
            align-items: center;
        }

        .top-bar .logo i {
            font-size: 2rem;
            margin-right: 10px;
        }

        .top-bar .nav-links {
            list-style: none;
            display: flex;
            gap: 25px;
            justify-content: center;
            
            flex: 1;
        }

        .top-bar .nav-links a {
            color: #ECF0F1;
            text-decoration: none;
            font-size: 1.2rem;
            transition: all 0.3s ease;
        }

        .top-bar .nav-links a:hover {
            color: #F39C12; /* Accord avec la couleur de la logo */
            transform: scale(1.05);
        }

        .top-bar .menu-icon {
            display: none;
        }

        @media (max-width: 768px) {
            .top-bar .nav-links {
                display: none;
                width: 100%;
                text-align: center;
                gap: 10px;
            }

            .top-bar .nav-links.active {
                display: block;
            }

            .top-bar .menu-icon {
                display: block;
                font-size: 2rem;
                cursor: pointer;
            }
        }

        /* Main Content */
        .content {
            margin-top: 70px;
            padding: 30px;
            background-color: #F8F9FA; /* Fond clair pour le contenu principal */
            flex: 1;
        }

        .content h1 {
            color: #2C3E50;
            font-size: 2.4rem;
            margin-bottom: 30px;
            font-weight: bold;
        }

        .content p {
            color: #555555;
            font-size: 1.1rem;
            line-height: 1.8;
        }

        /* Card */
        .card {
            background-color: #FFFFFF;
            border-radius: 8px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
        }

        .card .card-body {
            padding: 25px;
        }

        .card .card-title {
            font-size: 1.5rem;
            color: #2C3E50;
        }

        .card .card-text {
            color: #777777;
            font-size: 1.1rem;
        }
    </style>
</head>
<body>
    <!-- Top Bar -->
    <div class="top-bar">
        <div class="menu-icon" onclick="toggleMenu()">
            <i class="bi bi-list"></i> <!-- Icône hamburger -->
        </div>
        <div class="logo">
            <i class="bi bi-bootstrap"></i> <!-- Logo Bootstrap -->
            EmplyFlow
        </div>
        <ul class="nav-links">
            <li><a href="{{ path('admin_artisan_list') }}">employés</a></li>
            <li><a href="{{ path('profile_index') }}">Profils</a></li>
            <li><a href="{{ path('dashboard') }}">Tableau de Bord</a></li>
           
        </ul>
    </div>

    <!-- Main Content -->
    <div class="content">
        {% block body %}
        <h1>Bienvenue dans votre application</h1>
        <p>Explorez les fonctionnalités et utilisez le menu pour naviguer.</p>
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">Présentation des services</h5>
                <p class="card-text">Découvrez nos services, et comment ils peuvent vous aider à atteindre vos objectifs.</p>
            </div>
        </div>
        {% endblock %}
    </div>

    <script>
        // Fonction pour afficher/cacher le menu sur les petits écrans
        function toggleMenu() {
            var menu = document.querySelector('.top-bar .nav-links');
            menu.classList.toggle('active');
        }
    </script>
</body>
</html>
